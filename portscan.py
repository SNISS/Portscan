import socket

ip = input("Enter target ip: ")

door [80,21,22,444,20,8080,3636,]
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    if s.connect_ex((ip, doors)) == 0:
        print(doors, "Open")
        s.close()
